\documentclass{article}

\input{latexsrc/preamble/preamble}

%% begin.rcode
% #| label = "root",
% #| include = FALSE
% root <- rprojroot::is_rstudio_project
%% end.rcode

%% begin.rcode
% #| label = "knitr",
% #| include = FALSE,
% #| file = root$find_file(
% #|   "latexsrc",
% #|   "r-scripts",
% #|   "knitr.R"
% #| )
%% end.rcode

%% begin.rcode
% #| label = "biblatex",
% #| echo = FALSE,
% #| warning = FALSE,
% #| results = "asis"
% cat(
%   .PreambleBiblatexCombined(
%     path = root$find_file(
%       "latexsrc",
%       "bib"
%     ),
%     output_path = root$find_file(
%       "latexsrc"
%     )
%   )
% )
%% end.rcode

\renewcommand{\baselinestretch}{1}

\title{betaMC: Staging}
\author{Ivan Jacob Agaloos Pesigan}
\date{}

\begin{document}

\maketitle

%% begin.rcode
% #| echo = FALSE
% set.seed(42)
%% end.rcode

\section{Standardized Slopes}

%% begin.rcode
% #| eval = FALSE
% df <- nas1982
% object <- lm(QUALITY ~ NARTIC + PCTGRT + PCTSUPP, data = df)
% mvn <- BetaMC(object, type = "mvn")
% adf <- BetaMC(object, type = "adf")
% hc3 <- BetaMC(object, type = "hc3")
% summary(mvn)
% summary(adf)
% summary(hc3)
% coef(mvn)
% coef(adf)
% coef(hc3)
% vcov(mvn)
% vcov(adf)
% vcov(hc3)
% confint(mvn)
% confint(adf)
% confint(hc3)
%% end.rcode

\section{Multiple Correlation}

%% begin.rcode
% df <- nas1982
% object <- lm(QUALITY ~ NARTIC + PCTGRT + PCTSUPP, data = df)
% std_mvn <- BetaMC(object, type = "mvn")
% std_adf <- BetaMC(object, type = "adf")
% std_hc3 <- BetaMC(object, type = "hc3")
% mvn <- RSqBetaMC(std_mvn)
% adf <- RSqBetaMC(std_adf)
% hc3 <- RSqBetaMC(std_hc3)
% summary(mvn)
% summary(adf)
% summary(hc3)
% coef(mvn)
% coef(adf)
% coef(hc3)
% vcov(mvn)
% vcov(adf)
% vcov(hc3)
% confint(mvn)
% confint(adf)
% confint(hc3)
%% end.rcode

\section{Differences of Standardized Slopes}

%% begin.rcode
% df <- nas1982
% object <- lm(QUALITY ~ NARTIC + PCTGRT + PCTSUPP, data = df)
% std_mvn <- BetaMC(object, type = "mvn")
% std_adf <- BetaMC(object, type = "adf")
% std_hc3 <- BetaMC(object, type = "hc3")
% mvn <- DiffBetaMC(std_mvn)
% adf <- DiffBetaMC(std_adf)
% hc3 <- DiffBetaMC(std_hc3)
% summary(mvn)
% summary(adf)
% summary(hc3)
% coef(mvn)
% coef(adf)
% coef(hc3)
% vcov(mvn)
% vcov(adf)
% vcov(hc3)
% confint(mvn)
% confint(adf)
% confint(hc3)
%% end.rcode

\section{Partial Correlations}

%% begin.rcode
% df <- nas1982
% object <- lm(QUALITY ~ NARTIC + PCTGRT + PCTSUPP, data = df)
% std_mvn <- BetaMC(object, type = "mvn")
% std_adf <- BetaMC(object, type = "adf")
% std_hc3 <- BetaMC(object, type = "hc3")
% mvn <- PCorBetaMC(std_mvn)
% adf <- PCorBetaMC(std_adf)
% hc3 <- PCorBetaMC(std_hc3)
% summary(mvn)
% summary(adf)
% summary(hc3)
% coef(mvn)
% coef(adf)
% coef(hc3)
% vcov(mvn)
% vcov(adf)
% vcov(hc3)
% confint(mvn)
% confint(adf)
% confint(hc3)
%% end.rcode

\nocite{Lib-R-Manual-2022}

\printbibliography

\end{document}
